"""
Reformats code output into report.
"""

def join_chunks(code_inputs, code_outputs):
    print(code_inputs)
    print(code_outputs)

def produce_report(code_inputs, code_outputs):
    """
    Main function for transforming raw code
    """
    join_chunks(code_inputs, code_outputs)
